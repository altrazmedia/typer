# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on PR
"on": pull_request
jobs:
  build_and_preview:
    if: "${{ github.event.pull_request.head.repo.full_name == github.repository }}"
    runs-on: ubuntu-latest
    env:
      API_KEY: "${{ secrets.VITE_API_KEY }}"
      AUTH_DOMAIN: "${{ secrets.VITE_AUTH_DOMAIN }}"
      PROJECT_ID: "${{ secrets.VITE_PROJECT_ID }}"
      STORAGE_BUCKET: "${{ secrets.VITE_STORAGE_BUCKET }}"
      MESSAGING_SENDER_ID: "${{ secrets.VITE_MESSAGING_SENDER_ID }}"
      AAP_ID: "${{ secrets.VITE_AAP_ID }}"
    steps:
      - uses: actions/checkout@v2
      - run: npm install
      - run: npm run build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: "${{ secrets.GITHUB_TOKEN }}"
          firebaseServiceAccount: "${{ secrets.FIREBASE_SERVICE_ACCOUNT_TYPER_E55E5 }}"
          projectId: typer-e55e5
